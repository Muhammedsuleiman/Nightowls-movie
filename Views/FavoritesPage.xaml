<UserControl x:Class="NightOwls.Views.FavoritesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vwp="clr-namespace:WpfToolkit.Controls;assembly=VirtualizingWrapPanel">
    <Grid Margin="28,10,28,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" CornerRadius="12" Padding="18,14" Background="{StaticResource BrushSurface}"
                BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" Margin="0,4,0,16">
            <StackPanel Orientation="Horizontal">
                <TextBlock FontFamily="{StaticResource FontIcons}" Text="&#xE735;" FontSize="18"
                           Foreground="{StaticResource BrushGoldBright}" VerticalAlignment="Center"/>
                <TextBlock Text="Favorites" FontSize="21" FontWeight="Light"
                           Foreground="{StaticResource BrushTextPrimary}" VerticalAlignment="Center" Margin="12,0,0,0"/>
                <TextBlock Text="{Binding CountText}" FontSize="12.5" Foreground="{StaticResource BrushGold}"
                           VerticalAlignment="Center" Margin="14,2,0,0"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <ListBox ItemsSource="{Binding Favorites}" Background="Transparent" BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.VirtualizationMode="Recycling">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <vwp:VirtualizingWrapPanel ItemSize="210,170" Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <ContentPresenter/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Focusable" Value="False"/>
                        <Setter Property="AutomationProperties.Name" Value="{Binding TitleDisplay}"/>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>

            <Border Padding="40,30" Visibility="{Binding HasFavorites, Converter={StaticResource BoolToVisCInverted}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="&#xE734;" FontFamily="{StaticResource FontIcons}" FontSize="48"
                               Foreground="#55D9A441" HorizontalAlignment="Center"/>
                    <TextBlock Text="No favorites yet" FontSize="19" FontWeight="Light"
                               Foreground="{StaticResource BrushTextPrimary}" HorizontalAlignment="Center" Margin="0,14,0,0"/>
                    <TextBlock Text="Tap the star on any movie card to keep it here."
                               FontSize="13" Foreground="{StaticResource BrushTextSecondary}"
                               HorizontalAlignment="Center" Margin="0,7,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
